a - b == 5;

// This is a comment.

/*
 * This is a block comment.
 */

if (!("foo" != "bar")) {
	baz = [ "newline:\nfoo", "tab:\tbar", "carriage:\r hello", "backslash:\\ world",
	"double quote:\" quick", "literal tab:	brown", "fox\nhe said \"I jumped\""]; 	
}

complex_equation = 3 * 4+4 / -2 - 2;
complex_logic = (2 < 4) && a > b && (c <= d || (4.5 >= f) && !!true);

// Comment at end-of-file